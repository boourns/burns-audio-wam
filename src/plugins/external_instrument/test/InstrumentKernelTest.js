import {InstrumentKernel} from "../src/InstrumentDefinition.js"

describe("InstrumentKernel", () => {
    it("Tests", async () => {
        const def = {
            controlGroups: []
        }

        const a = new InstrumentKernel(def)      
    })
})
